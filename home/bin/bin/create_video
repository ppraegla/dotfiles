#!/usr/bin/env bash
set -e
set -u
set -o pipefail

FRAMES="$1"
FOLDER="$2"

ffmpeg -r $FRAMES -f image2 -pattern_type glob -i "$FOLDER/*.png" -vcodec libx264 -crf 18 -pix_fmt yuv420p -vf "crop=trunc(iw/2)*2:trunc(ih/2)*2" $FOLDER/output.mp4
