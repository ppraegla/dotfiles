#!/usr/bin/env bash
# Checkout a pull request. The commands assume that your fork is the origin and the PR is against the upstream.
set -e
set -u

PRNUMBER=$1
git fetch upstream pull/$PRNUMBER/head:pr/$PRNUMBER && git checkout pr/$PRNUMBER
